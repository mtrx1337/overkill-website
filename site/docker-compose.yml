version: '3.7'
services:
    site:
        build: .
        image: site
        container_name: site
        restart: always
        volumes:
            - /var/www:/var/www

    nginx:
        image: nginx
        container_name: nginx
        ports:
            - "8080:8000"
        depends_on:
            - site
